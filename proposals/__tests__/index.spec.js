

describe("proposals.index", () => {
    describe("foo", () => {

        it("should return 'bar'", () => {
            const index = require("../index");
            expect(index.foo()).toBe("bar");
        });

    });
});